services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: >
      bash -lc "pip install -e '.[dev]' && pre-commit install && tail -f /dev/null"
